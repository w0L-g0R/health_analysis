# from dependency_injector import containers, providers

# from api.health.handler import HealthHandler


# class HealthContainer(containers.DeclarativeContainer):
#     health_handler = providers.Singleton(HealthHandler)
